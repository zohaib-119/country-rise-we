/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['plus.unsplash.com', 'res.cloudinary.com'], // Add the allowed domains here
  },
};

export default nextConfig;

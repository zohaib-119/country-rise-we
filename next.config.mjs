/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['plus.unsplash.com', 'res.cloudinary.com', "media.istockphoto.com", "img.freepik.com"], // Add the allowed domains here
  },
};

export default nextConfig;
